#include <iostream>

#include "tree.h"

int main () {
  Tree t(std::cout);
  t.process_symbol('a');
  t.process_symbol('b');
  // t.process_symbol('a');
  // t.process_symbol('a');
  // t.process_symbol('a');
  // t.process_symbol('a');
  // t.process_symbol('b');
  // t.process_symbol('b');
  // t.process_symbol('b');
  // t.process_symbol('b');
  // t.process_symbol('b');
  // t.process_symbol('b');
  // t.process_symbol('b');
  // t.process_symbol('b');





  // t.process_symbol('a');
  // t.process_symbol('a');
}
